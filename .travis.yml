language: node_js

node_js:
  - stable

install:
  - npm install

before_script:
  - psql -c 'create database more-recipes-test;' -U postgres
  - NODE_ENV=test ./node_modules/.bin/sequelize db:migrate

script:
  - npm test

after_success: npm run coverage
